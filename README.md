# CoverLetterApplication
Takes the user details, the Job description, and resume and generates a tailored cover letter for that job which can be saved as a pdf. User can add in extra initial details as well as make adjustments after the initial generation. The only limitation is that the pdf is bland and does not have templates.
